Usage example:

```
target 'YOUR-APP' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fanmeter-ios-testapp
  pod 'fanmeter_sdk_ios', '~> 2.0.0'

end
```